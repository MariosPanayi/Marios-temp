[Mean, Median, SEM, N] = aggregate(data, groups)

% data = vector with subjects as columns [can't
% groups = cell array with a group represented as a column, each cell
% containing a vector of column numbers associated with the subjects in
% that group

Mean = [];
Median = [];
SEM = [];
N = [];

for i =  1:size(groups,2)
    Mean(i) = mean(mean(data(1:6,groups{i})));
end
